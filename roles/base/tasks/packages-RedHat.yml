---
- name: Install base packages block
  become: true
  block:
    - name: Install rpmfusion free repository
      ansible.builtin.dnf:
        name: "{{ base_rpmfusion_free_repo }}"
        state: present
        disable_gpg_check: true

    - name: Install rpmfusion nonfree repository
      ansible.builtin.dnf:
        name: "{{ base_rpmfusion_nonfree_repo }}"
        state: present
        disable_gpg_check: true

    - name: DNF conf changes
      ansible.builtin.blockinfile:
        path: /etc/dnf/dnf.conf
        block: |
          max_parallel_downloads=10
          fastestmirror=True
        backup: true

    - name: Upgrade all packages
      ansible.builtin.dnf:
        name: "*"
        state: latest
        update_cache: true

    - name: Install base packages
      ansible.builtin.dnf:
        name:
          - ansible
          - atuin
          - bat
          - btop
          - curl
          - "@Development Tools"
          - dnf-plugins-core
          - dnf-utils
          - fastfetch
          - fish
          - fzf
          - git
          - git-delta
          - golang
          - kernel-devel
          - nmap
          - python3-psutil
          - traceroute
          - tree
          - util-linux-user
          - zsh
        state: present
# End of base packages block
