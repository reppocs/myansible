---
# tasks file for gnome

- name: Include gnome packages task
  ansible.builtin.include_tasks: "packages.yml"

- name: Include gnome settings task
  ansible.builtin.include_tasks: "settings.yml"

# EOF
